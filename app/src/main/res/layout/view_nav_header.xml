<?xml version="1.0" encoding="utf-8"?>
<graduation.mcs.ui.view.NavHeaderView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@drawable/side_nav_bar"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"
    android:gravity="bottom"
    >

    <ImageView
        android:id="@+id/nav_header_avatar"
        android:layout_width="@dimen/nav_header_avatar_width"
        android:layout_height="@dimen/nav_header_avatar_height"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:src="@mipmap/bili_default_avatar"
        android:scaleType="fitCenter"
        />


    <LinearLayout
        android:id="@+id/nav_header_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:layout_below="@id/nav_header_avatar"
        android:orientation="vertical"
        android:visibility="gone"
        >

        <TextView
            android:id="@+id/nav_header_login_nickname"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@android:style/TextAppearance.Widget.TextView"
            android:text="张三"
            />

        <TextView
            android:id="@+id/nav_header_login_phone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="15521108281"
            android:textColor="@color/colorWindow"
            />

    </LinearLayout>

    <TextView
        android:id="@+id/nav_header_unlogin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:layout_below="@id/nav_header_avatar"
        android:text="@string/nav_header_unlogin"
        android:textSize="@dimen/medium_text"
        android:textColor="@color/colorWindow"
        />

</graduation.mcs.ui.view.NavHeaderView>